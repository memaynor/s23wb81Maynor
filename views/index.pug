extends layout

block content
  h1= title
  p Welcome to #{title} woohoo making a website
  button(type="button", class="btn btn-primary", onclick="doReduce()") Press Me
  div#reduce Reduce the value.
  a(href='/art') View Art Supplies


  script.
    let count = 20;
    let change = -1;

    function doReduce() {
      count += change;
      change--;
      document.getElementById("reduce").textContent = `Count ${count} with change ${change}`;
    }
